## 1. Preserve list-tail source lines

- [x] 1.1 Replace independent list-tail whitespace helpers with ordered source-backed runs; verify projections and round trips for spaces, tabs, LF/CRLF, nesting, formatted text and following blocks.
- [x] 1.2 Add GPUI regressions for nested-list continuation/exit/repeated Enter, pointer placement, Unicode input and undo; verify painted caret rows and exact canonical source.

## 2. Integration verification

- [x] 2.1 Run root-package tests, formatting and diff checks, validate the OpenSpec change, and document verified coverage.
