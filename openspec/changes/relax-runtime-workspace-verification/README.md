# relax-runtime-workspace-verification

Launch the WeChat publishing workspace after a minimal runtime gate (manifest provenance + index.html digest) so installer upgrade leftovers never block publishing; release-time bundle verification stays fully strict.
