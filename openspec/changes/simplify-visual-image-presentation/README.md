# simplify-visual-image-presentation

Visual Edit now shows images with a single read-only caption line (Title when present, otherwise Alt Text) instead of the editable Alt Text / Destination / Title field table. Editing any image field requires switching to source editing.
